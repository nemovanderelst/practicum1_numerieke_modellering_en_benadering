function [Q,R] = herGS(A)
    [m,n] = size(A);

    V = A;
    W = zeros(m, n);
    Q = zeros(m, n);
    R = zeros(n, n);
    S = zeros(m, n);

    for j=1:n
        R(1:(j-1), j) = Q(:,1:(j-1))'*A(:,j);
        V(:,j) = V(:,)
    end
end

